config:
  target: 'http://localhost:3003'
  phases:
    - duration: 60
      arrivalRate: 1500
scenarios:
  - flow:
    - get:
        url: "/{{$randomNumber(1, 10000000)}"
    - get: 
        url: "/amenities/{{$randomNumber(1, 10000000}}"
